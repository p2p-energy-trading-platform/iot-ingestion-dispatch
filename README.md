# IoT Ingestion & Dispatch Service

Consumes IoT telemetry from Kafka and persists it into Redis (hot storage) and TimescaleDB (warm storage). Also documents the planned Dispatch Service and gRPC interfaces (not yet implemented).

See [plan.md](./plan.md) for the full design.

**Status:** Plan under review - implementation not yet started.
